import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '21.ionic-fix-keyboard-pushing-content-up',
  webDir: 'www'
};

export default config;
